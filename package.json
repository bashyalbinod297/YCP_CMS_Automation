{
  "name": "ycp-cms-automation",
  "version": "1.0.0",
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require ./support/hooks.ts --require ./steps/**/*.ts --format progress"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.0.0",
    "@playwright/test": "^1.56.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
